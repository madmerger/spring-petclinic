name: Deploy and Test Cluster

on:
  push:
    branches: [main]
    paths:
      - 'k8s/**'
  pull_request:
    branches: [main]
    paths:
      - 'k8s/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  deploy-and-test-cluster:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build container image
        run: ./mvnw spring-boot:build-image -DskipTests -Dspring-boot.build-image.imageName=petclinic:test

      - name: Create k8s Kind Cluster
        uses: helm/kind-action@v1
        with:
          cluster_name: kind

      - name: Load image into Kind cluster
        run: kind load docker-image petclinic:test --name kind

      - name: Deploy application with test image
        run: |
          kubectl apply -f k8s/db.yml
          # Replace image, set imagePullPolicy to Never, and remove resource limits for CI
          cat k8s/petclinic.yml \
            | sed 's|image: dsyer/petclinic|image: petclinic:test|' \
            | sed 's|imagePullPolicy: Always|imagePullPolicy: Never|' \
            | sed '/resources:/,/cpu: "500m"/d' \
            | kubectl apply -f -

      - name: Wait for Pods to be ready
        run: |
          kubectl wait --for=condition=ready pod -l app=demo-db --timeout=180s
          kubectl wait --for=condition=ready pod -l app=petclinic --timeout=300s

      - name: Smoke test - check application health
        run: |
          kubectl port-forward svc/petclinic 8080:80 &
          sleep 10
          curl -f --retry 5 --retry-delay 5 http://localhost:8080/actuator/health || exit 1
